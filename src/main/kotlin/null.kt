 fun main() {
    var favoriteActor: String? =null
     if (favoriteActor != null)
         println("The number of characters i your favorite actor's name is ${favoriteActor}")
     else {
         println("You didn't input a name")
     }
    // println(favoriteActor)
    // println(favoriteActor!!.length)

    // favoriteActor = null
     //(favoriteActor)

        }

// fun main() {
  //   var number:Int? = 10
   //  println(number)
//
    // number = null
     //println(number )
// }